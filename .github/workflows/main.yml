name: P4A Android APK Build

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
              git \
              zip \
              unzip \
              autoconf \
              libtool \
              pkg-config \
              zlib1g-dev \
              libncurses-dev \
              cmake \
              libffi-dev \
              libssl-dev \
              build-essential \
              automake \
              gettext
          pip install --upgrade pip
          pip install cython "setuptools<60.0"

      - name: Install and configure python-for-android
        run: |
          pip install python-for-android
          # 下载所有需要的平台和工具
          p4a sdk-tools install --platforms android-34 --build-tools 34.0.0
          
      - name: Build with python-for-android
        run: |
          p4a apk --name "A股异动监控" \
                  --package "org.test.stockmonitor" \
                  --version 0.1 \
                  --bootstrap sdl2 \
                  --requirements "python3,kivy,pandas,akshare,requests,lxml,charset-normalizer,idna,openssl,pyopenssl" \
                  --arch arm64-v8a \
                  --release \
                  --permission INTERNET \
                  --private .

      - name: Find and rename APK
        run: |
          find . -type f -name "*.apk" -exec mv {} A-Stock-Monitor.apk \;

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: stockmonitor-apk
          path: A-Stock-Monitor.apk
